#include<stdio.h>
#include <stdlib.h>

int main(){

    /* if we add const before int variable the we cannot modify the variable */

    const int num = 10;

    printf(" %d\n ", num );

    return 0;

}